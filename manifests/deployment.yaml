# Generated by IBM TransformationAdvisor
# Tue Oct 02 14:56:22 UTC 2018

apiVersion: v1
kind: Service
metadata:
  name: jpetstorewar
  labels:
    app: jpetstorewar
spec:
  ports:
    - port: 9080
  selector:
    app: jpetstorewar
  type: NodePort

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: jpetstorewar
  name: jpetstorewar
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: jpetstorewar
    spec:
      containers:
      - image: mycluster.icp:8500/default/jpetstorewar:latest
        name: jpetstorewar
        env:
          - name: WLP_LOGGING_CONSOLE_FORMAT
            value: json
          - name: WLP_LOGGING_CONSOLE_LOGLEVEL
            value: info
          - name: WLP_LOGGING_CONSOLE_SOURCE
            value: message,trace,accessLog,ffdc
        ports:
        - containerPort: 9080
          protocol: TCP
